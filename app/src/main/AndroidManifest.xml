<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="guideku.bisfor.guideku">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screen.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screen.login.LoginActivity" />
        <activity android:name=".screen.register.RegisterActivity" android:theme="@style/Theme.AppCompat.Light.Dialog" />
        <activity android:name=".screen.profile.ProfileActivity" />
        <activity android:name=".screen.main.MainActivity"  />
        <activity android:name=".screen.chat.detail.ChatDetailActivity"></activity>
    </application>

</manifest>